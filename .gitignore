# large files
spx_w/

# python metadata
__pycache__/
.DS_Store
*.pkl

# storing api keys
*.key
api_key.py

# generated pipeline outputs (reproducible via the package / notebook)
artifacts/
spx_w.zip

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/build.log
# untouched copy of the pre-audit paper (local reference only)
paper/_original/
